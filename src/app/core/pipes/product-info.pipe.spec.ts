import { ProductInfoPipe } from './product-info.pipe';

describe('ProductInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
