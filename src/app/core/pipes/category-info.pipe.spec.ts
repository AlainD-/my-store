import { CategoryInfoPipe } from './category-info.pipe';

describe('CategoryInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
